# Valhalla Data

`custom_files/` is mounted into the Valhalla container as `/custom_files`.

Place local OSM PBF extracts here. The current local extract is:

```text
custom_files/cyprus-latest.osm.pbf
```

The `custom_files/` directory is ignored by git because it contains local source data and generated Valhalla routing files.
